#include <stdio.h>

int main() {
    // find the perimeter of a rectangle
    // length 
    // breadth 
    // perimeter = 2 x (length + breath);

    float length = 13.5;
    float breath = 17.34;


    float perimeter = 2 * (length + breath);

    // output the result
    printf("Perimeter = %f\n", perimeter);

    return 0;
}