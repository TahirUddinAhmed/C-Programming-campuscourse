#include <stdio.h>

int main() {
    // Variable declaration (create)
    float tax;

    // initialization
    tax = 18.34;

    // printf("tax = %.2f\n", tax);

    // Update the value 
    tax = 78.56;
    printf("tax is now %.2f\n", tax);


    return 0;
}