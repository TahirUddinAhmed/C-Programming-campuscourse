#include <stdio.h>

int main() {
     // find the area of rectangle 
     float length = 8.45;
     float breath = 5.67;

     float area = length * breath;

     printf("the area is %f", area);

    return 0;
}