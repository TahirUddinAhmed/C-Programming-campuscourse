#include <stdio.h>

int main() {
    // integer to character
    // char ch = 64; // a


    // float to integer
    float a = 3.1;
    int b = a;

    // explicit conversion
    float f = 13.4;
    int i = (int) f;

    // print -> ch
    printf("%d\n", (int) f);
    return 0;
}