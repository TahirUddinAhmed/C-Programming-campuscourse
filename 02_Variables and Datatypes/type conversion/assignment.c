#include <stdio.h>

int main() {
    // Given integer and float values
    int intValue = 10;
    float floatValue = 5.7;

    // Perform the following tasks:
    // 1. Convert intValue to float and store it in resultFloat.
    // 2. Convert floatValue to int and store it in resultInt.
    // 3. Print all the values


    return 0;
}
